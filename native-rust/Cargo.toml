[package]
name = "algovoi"
version = "1.2.0"
edition = "2021"
description = "AlgoVoi payment adapter — Tier 1 (one-shot checkout) + Tier 2 (standing-authority recurring) — zero external dependencies"
license = "MIT"

# No [dependencies] — stdlib only
